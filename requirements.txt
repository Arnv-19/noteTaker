# PDF Annotator dependencies (Python 3.x)
PyQt6            # GUI framework
PyQt6-WebEngine  # Obsidian-style Markdown preview with inline images & video
PyMuPDF          # PDF rendering and annotation (imported as "fitz")
pikepdf          # PDF repair / structural rebuild (repair.py)
